# BidMatch 트러블슈팅

이 문서는 실제 커밋, 테스트, 현재 코드로 확인된 문제를 중심으로 정리했습니다. 면접에서는 모든 사례를 말하기보다 지원 직무에 맞는 2~3개를 선택합니다.

## 1. 팀 병렬 개발 중 Flyway 버전 충돌

### 문제

여러 팀원이 동시에 DB 기능을 추가하면서 동일한 Flyway 버전이 중복됐고, 기존 개발 DB에는 이미 일부 마이그레이션이 적용된 상태였습니다. 파일을 단순 수정하면 체크섬 충돌과 환경별 스키마 차이가 발생할 수 있었습니다.

### 원인

- 기능 브랜치별로 다음 마이그레이션 번호를 독립적으로 선택
- 빈 DB와 기존 개발 DB의 적용 이력 차이
- Entity 변경과 레거시 컬럼의 이름 차이

### 판단

이미 공유 DB에 적용된 SQL은 수정하지 않고, 새 버전으로 이동하거나 후속 보정 마이그레이션을 추가해야 한다고 판단했습니다.

### 해결

- 중복된 V24 관심조건 마이그레이션을 V31로 이동
- 사용자 면허 V32 충돌을 V33으로 이동
- 레거시 컬럼 호환을 위한 V8·V9 보정 추가
- `IF EXISTS`·`IF NOT EXISTS`를 고려해 기존 DB와 빈 DB 모두에서 실행 가능하도록 구성

### 검증

- 관련 커밋: `0751d15`, `0accc88`, `2576406`
- Backend Gradle 테스트 성공
- 현재 Flyway 목록에서 중복 버전 없음

### 이력서 문장

> 병렬 개발 중 발생한 Flyway 버전·체크섬 충돌을 기존 적용 이력을 보존하는 후속 마이그레이션 방식으로 해결하여 개발 DB와 빈 DB의 스키마 일관성을 확보했습니다.

## 2. 외부 국세청 API 실패가 기업 가입을 막는 문제

### 문제

기업 가입 과정이 국세청 사업자등록정보 진위 확인 API에 의존하면서 외부 API의 응답 형식 변화나 장애가 가입 전체 실패로 이어질 수 있었습니다.

### 원인

- 외부 API 오류와 실제 사업자 정보 불일치를 같은 실패로 처리
- 프론트와 Backend의 실패 응답 계약 불일치
- 원인 파악에 필요한 진단 정보 부족

### 해결

- 사업자 검증 응답 계약을 명확히 분리
- 외부 API 호출 진단 로그 추가
- 기업 인증 실패와 외부 서비스 일시 장애를 구분
- 재인증 신청과 관리자 검토 흐름으로 연결
- 화면에서 사용자에게 재시도 또는 검토 접수 안내 제공

### 검증

- 관련 커밋: `9068769`, `92d844e`, `ab8ce59`
- `BusinessValidationServiceTest`와 인증 관련 테스트 존재

### 면접 핵심

외부 API 실패를 내부 트랜잭션 실패와 동일하게 취급하지 않고, 사용자가 다음 행동을 선택할 수 있는 상태로 변환한 사례입니다.

## 3. OCR 엔진과 서비스 API 계약 불일치

### 문제

OCR 엔진이 사업자등록증에서 값을 추출하더라도 Backend와 Frontend가 기대하는 필드명·형식이 다르면 자동입력이 실패했습니다. OCR 실패 원인도 운영에서 확인하기 어려웠습니다.

### 원인

- AI OCR 응답과 기업 가입 폼의 데이터 계약 차이
- 이미지 인식 실패와 Backend 중계 실패가 같은 오류로 표시
- OCR 요청·응답 이력 부재

### 해결

- Backend OCR 중계 Client와 전용 응답 DTO 구현
- 파일·응답 검증과 OCR 전용 예외 응답 추가
- 회원가입·기업정보 화면에 자동입력 연결
- 인식 주소를 우편번호 검색과 연결
- `business_registration_ocr_logs` 성격의 별도 이력 저장
- 챗봇 로그와 OCR 로그 테이블 책임 분리

### 검증

- 관련 커밋: Backend `7e9a2fd`, `a932ed7`; Frontend `e09db78`, `ff0dd33`; Bruno `cf75476`
- `BusinessOcrClientTest`, `BusinessOcrResponseTest` 존재

### 기여 범위

OCR 모델 자체가 아니라 OCR 서비스 연동, 계약, UI 자동입력과 운영 로그를 구현한 사례로 설명합니다.

## 4. 나라장터 면허 조회 결과가 변경되는 문제

### 문제

이전에 조회된 면허가 다음 나라장터 조회에서 사라졌을 때 즉시 삭제하면 과거 상태와 사용자 수동 등록 정보를 잃게 됩니다. 반대로 계속 활성으로 두면 자격 진단에 잘못 반영될 수 있습니다.

### 원인

- 외부 조회 데이터는 시점에 따라 달라질 수 있음
- 나라장터 조회 면허와 사용자 직접 등록 면허의 출처가 다름
- 삭제와 일시 미확인을 구분하지 않은 상태 모델

### 해결

- 면허 출처와 상태를 분리
- 나라장터에서 누락된 면허를 `NOT_FOUND`로 전환하고 확인 시각 저장
- 수동 등록 면허는 나라장터 조회에 없더라도 활성 유지
- 사용자가 누락 면허를 직접 등록 면허로 전환하거나 제거하도록 처리
- 여러 면허 중 실제 누락된 항목만 비활성화

### 검증

`UserBusinessLicenseServiceTest`에서 다음 시나리오를 검증합니다.

- 누락 면허의 `NOT_FOUND` 전환
- 반복 조회 시 상태 유지
- 수동 면허로 재활성화
- 사용자의 제거 처리
- 수동 면허 활성 유지
- 복수 면허 중 누락 항목만 비활성화

### 이력서 문장

> 나라장터 조회 면허와 사용자 직접 등록 면허를 출처·상태 기반으로 모델링하여 외부 조회 결과 변경에도 이력과 자격 판정의 일관성을 유지했습니다.

## 5. 비밀번호와 계정 보안 정책의 화면·서버 불일치

### 문제

회원가입, 비밀번호 재설정, 내 정보 변경 화면에서 서로 다른 비밀번호 규칙을 사용하거나 만료된 재설정 요청을 허용할 가능성이 있었습니다.

### 해결

- 공통 비밀번호 정책 클래스 도입
- 비밀번호 재설정 만료 검증
- 현재 비밀번호와 동일한 새 비밀번호 거부
- 비밀번호 변경 시각 저장
- 탈퇴 시 이메일·이름·전화번호·비밀번호 비식별화
- 프론트에서도 동일한 검증 메시지와 입력 흐름 적용

### 검증

- `PasswordResetServiceTest`
- `UserSecurityServiceTest`
- `LoginFailureHandlerTest`
- `SignupRequestValidationTest`

## 6. 불필요한 IP 수집과 개인정보처리방침 불일치

### 문제

회원가입 동의 정보에 IP를 저장하는 로직과 개인정보처리방침 문구가 존재했으나 서비스에 반드시 필요한 정보가 아니었습니다.

### 판단 및 해결

최소 수집 원칙에 따라 Backend의 IP 저장 로직과 Entity 필드를 제거하고, Frontend 개인정보처리방침의 IP 수집 문구도 함께 삭제했습니다.

### 검증

- Backend `94e9110`
- Frontend `a056415`

### 면접 핵심

기능 구현뿐 아니라 실제 수집 목적과 문서의 일치 여부를 검토해 불필요한 개인정보를 제거한 사례입니다.

## 7. 실시간 알림과 이력 조회의 데이터 구조 분리

### 문제

관리자 알림 발송 이력만으로는 사용자별 관련 공고, 읽음 상태, 상세 화면과 실시간 갱신을 안정적으로 제공하기 어려웠습니다.

### 원인

- 발송 단위 로그와 사용자에게 표시할 알림 항목의 책임이 혼재
- 실시간 이벤트만 사용하면 재접속 후 과거 알림을 복구하기 어려움
- 목록·상세·읽음 API 부재

### 해결

- `NotificationLog`와 사용자별 `NotificationLogItem` 역할 분리
- 목록·상세·단건 읽음·전체 읽음 API 구현
- SSE 구독 API와 사용자별 연결 관리 Service 구현
- DB 저장 후 이벤트를 발행해 실시간 UI 갱신
- SSE 예외 처리와 재연결 가능한 이력 조회 제공

### 검증

- Backend `3424684`
- Frontend `1c3a6e3`
- Bruno `97567fb`
- 현재 `/api/user/notifications/stream`과 읽음 API 존재

### 이력서 문장

> 알림 발송 로그와 사용자별 읽음 항목을 분리하고 SSE와 REST 이력 조회를 결합해 실시간성과 재접속 복구를 함께 지원했습니다.

## 8. 고객센터 목록·권한·상태 관리 복잡도

### 문제

비회원 문의, 로그인 사용자 문의, 관리자 답변이 한 기능에 모이면서 조회 권한, 페이징, 문의 취소와 답변 상태가 복잡해졌습니다.

### 해결

- 비회원 등록 API와 회원 문의 API 분리
- 로그인 사용자는 자신의 문의만 목록·상세 조회
- 페이징 응답 도입
- 수정과 취소 상태 전이 추가
- 관리자 답변·상태 변경과 게시물 삭제 API 보완
- Frontend를 공지·FAQ·1:1 문의 탭으로 구성

### 검증

- `PublicInquiryServiceTest`
- `UserInquiryServiceTest`
- `SupportNoticeServiceTest`
- `AdminOperationsServiceTest`

## 9. 자사 실적 날짜의 비정상 연도 입력

### 문제

브라우저 날짜 입력만 신뢰하면 지나치게 긴 연도나 비정상 범위가 API에 전달될 수 있었습니다.

### 해결

- 등록·수정 요청 DTO에 네 자리 연도 범위 검증 추가
- Frontend에서 입력 검증과 오류 메시지 적용
- 목록을 준공일 기준으로 정렬

### 검증

- Backend `427163b`
- Frontend `b6ba4d6`
- `BusinessPerformanceRequestValidationTest`

## 10. 챗봇 검색 결과의 권한과 신뢰성

### 문제

단순 벡터 검색 결과를 그대로 LLM에 전달하면 검토되지 않은 FAQ, 사용자 권한과 맞지 않는 안내, 낮은 유사도의 잘못된 답변이 제공될 수 있었습니다.

### 원인

- 승인 상태와 접근 권한을 고려하지 않은 검색
- 모호한 질문과 서비스 외 질문의 강제 답변
- LLM 실패 시 안전한 대체 답변 부재

### 해결

- 승인 FAQ와 검토대기 FAQ 데이터 분리
- 역할별 `allowedRoles` 필터 적용
- 낮은 유사도는 상담 연결 안내, 서비스 외 질문은 별도 안전 응답
- 단일 고신뢰 FAQ는 승인 답변을 바로 반환
- 복합 의도 질문은 최대 두 개의 검증된 FAQ를 조합
- LLM이 불완전·비한국어·실패 응답을 반환하면 승인 FAQ로 fallback
- 짧고 모호한 질문은 세부 상황을 다시 질문

### 검증

- 승인 FAQ 최소 49건
- 검색 평가 케이스 최소 68건
- 역할 필터, 낮은 유사도, 복합 의도, fallback 테스트

## 11. 챗봇 로그와 LangSmith의 민감정보 노출

### 문제

챗봇 질문에는 이메일, 전화번호, 사업자번호, 주민번호, 계좌번호, 비밀번호 등이 포함될 수 있어 DB 로그와 외부 추적 도구에 원문을 저장하면 개인정보가 노출될 수 있었습니다.

### 해결

- 민감정보 유형별 마스킹 규칙 구현
- 체크섬과 주변 문맥을 사용해 일반 숫자·공고번호의 과도한 마스킹 방지
- DB 저장 전 요청·응답·오류 메시지 마스킹
- LangSmith 입력·출력·오류에도 동일한 마스킹 적용
- 이미 마스킹된 값에 다시 적용해도 결과가 변하지 않는 멱등성 확보

### 검증

`test_chatbot_log.py`와 `test_chatbot_tracing.py`에서 이메일, 전화번호, 사업자번호, 주민번호, 계좌번호, 비밀번호, 중복 마스킹을 검증합니다.

### 이력서 문장

> 챗봇 DB 로그와 LangSmith 추적 데이터에 공통 민감정보 마스킹을 적용하고 문맥·체크섬 검증으로 과도한 마스킹을 방지했습니다.

## 12. 자가 자격 진단: 전체 문서 처리로 인한 시간 초과

### 문제

첨부문서 전체를 임베딩하고 로컬 LLM에 전달하면서 분석 시간이 길어지고 Qwen 호출 제한시간을 초과했습니다. 동일 공고를 반복 실행하면 같은 비용이 다시 발생했습니다.

### 해결

- 20MB 파일 크기 제한
- 900자 청크와 120자 중첩
- 자격 관련 키워드·의무 표현을 기준으로 첨부파일별 최대 3개 청크 선별
- BGE-M3로 유형별 Top-K 3개 검색
- LLM 전달 근거를 최대 4개·1,600자로 제한
- 공고별 비동기 상태와 최대 4회 시도
- 원문 해시·모델·분석 버전 기반 결과 캐시
- 중복 요청 시 기존 완료 결과 재사용

### 검증

- Backend Flyway V45·V46
- AI `document_eligibility.py`
- Backend·AI 문서 자격진단 테스트

## 13. 자가 자격 진단: 일반 안내 문구의 참가조건 오인

### 문제

하도급 위반 시 제재, 계약 일반조건, 평가 가점, 서식 설명, 재무평가 제출자료 등이 실제 참가조건으로 분류됐습니다.

### 해결

- 일반 계약기준 첨부문서 제외
- 자격 유형별 키워드와 의무 표현 검증
- 참여 가능 여부에 직접 영향을 주지 않는 제재·정산·평가 문구 제외
- 긴 원문에서 실제 요구조건만 완결된 문장으로 정리
- 원문 근거를 함께 반환해 사용자가 확인 가능하도록 구성

### 검증 사례

- 하도급 위반 제재 문구 제외
- 하도급 대금 관리 문구 제외
- 건설기계 대여금 보증 정산을 입찰보증으로 분류하지 않음
- 재무평가 서류를 필수 인증으로 분류하지 않음
- 실적 공증 안내를 실적 기준으로 오인하지 않음

## 14. 자가 자격 진단: Qwen 비정상·모호한 출력

### 문제

Qwen이 설명문, 유형 카탈로그 전체, 근거 없는 유형 또는 잘린 응답을 반환해 잘못된 근거가 연결될 수 있었습니다.

### 해결

- 모델 역할을 최종 판정이 아닌 근거 유형 분류로 제한
- `근거ID|TYPE` 형식만 파싱
- 요청에 포함된 근거 ID와 허용 유형만 수용
- 근거 후보가 모호하거나 원문과 맞지 않으면 폐기
- 모델 응답을 사용할 수 없으면 규칙 기반 근거 추출 fallback
- 사용자용 문장은 원문에서 다시 구성

### 검증

- 출력 형식, 유형 카탈로그 에코, 비허용 유형, 근거 없는 ID 관련 테스트
- 문서 원문과 연결된 결과만 Backend 진단에 병합

## 15. 자가 자격 진단: 구조화 조건과 첨부문서 불일치

### 문제

구조화 데이터에는 지역 제한이 없거나 특정 시·군으로 저장됐지만 첨부문서에는 도 단위 조건이 존재하는 사례가 있었습니다. 복수 면허도 ‘중 하나’와 ‘모두 등록’의 의미가 달랐습니다.

### 해결

- 첨부문서 원문을 이용해 지역·면허 조건 재판정
- 짧은 도·광역시 이름 정규화
- 더 구체적이거나 잘못 저장된 구조화 지역을 문서 근거로 보정
- 면허 OR 조건과 전체 등록 조건 구분
- 기존 조건과 문서 근거가 같으면 중복 카드 대신 근거만 결합
- 결과 변경 시 충돌·재판정 경고 제공

### 검증

`EligibilityDocumentDiagnosisServiceTest`에서 다음을 검증합니다.

- 문서 지역 충돌 시 점수 재계산
- 누락 문서 면허의 미충족 판정
- 동일 면허 근거 중복 방지
- ‘모두 등록’ 면허 판정
- 도 단위 지역 병합과 과도하게 구체적인 지역 보정

## 16. 자가 자격 진단: 일부 파일 또는 LLM 실패로 전체 진단 중단

### 문제

XLSX·ZIP 등 미지원 형식, 손상 문서, 용량 초과 또는 Ollama 중단이 발생하면 첨부문서 진단 전체가 실패할 수 있었습니다.

### 해결

- 파일별 예외를 수집하고 나머지 문서 분석 계속
- 지원하지 않는 형식, 용량 초과, 내부 오류를 사용자용 사유로 변환
- 내부 오류 상세는 숨기고 원문 링크 제공
- LLM 실패 시 기본 지역·면허·마감 진단 유지
- 분석 상태를 `PENDING`, `PROCESSING`, `COMPLETED`, `PARTIAL`, `FAILED`로 관리
- 마감 후 3일이 지난 파생 문서 데이터 정리

### 면접 핵심

AI 기능을 필수 트랜잭션에서 분리하여 일부 실패가 서비스 핵심 기능 전체로 전파되지 않도록 만든 graceful degradation 사례입니다.

## 17. 현재 남은 개선 과제

### 사용자 프론트 번들 크기

- production build는 성공하지만 JavaScript 번들이 약 839kB로 Vite의 500kB 경고를 초과
- 향후 라우트 단위 `dynamic import`, vendor chunk 분리, 무거운 라이브러리 지연 로딩 검토

### AI 테스트 의존성 경고

- AI 테스트 242건은 통과
- Starlette TestClient의 httpx 사용 방식이 폐기 예정이라는 경고 1건 존재
- 향후 FastAPI·Starlette·httpx 호환 버전 정리 필요

측정하지 않은 성능 향상률이나 정확도 수치는 추가하지 않습니다.
